const Joi = require('joi');

/*
JOI Validation script is for JSON Pillcap Object Below
** NOTE ** Optionals also TODO: update ranges appropriately

"data": {}
*/

function validatePillCap(measurementData) {
    return undefined
};

module.exports = validatePillCap;